#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* shortestPalindrome(char* s) {
	return (NULL);
}

int main(int argc, char *argv[])
{
    char *ret = shortestPalindrome(argv[1]);
	printf("%s\n", ret);
    return 0;
}

